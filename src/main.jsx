import ReactDOM from 'react-dom/client';//this is built right into react

import App from './App';

import './index.css';
ReactDOM.createRoot(document.getElementById('root')).render(<App />);//react is going to hook into a single div on our HTML and then populate with all of the great content we are about to create